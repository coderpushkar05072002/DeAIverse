import React from "react";

export default function Hero() {
  return (
    <section className="aur-hero">
      <div className="aur-hero-panel">
        <h1 className="aur-hero-title">
          <span>Compute your AI tasks —</span><br/>
          <span className="aur-hero-highlight">decentralized &amp; auditable</span>
        </h1>
        <p className="aur-hero-sub">
          Create compute jobs, hire providers, and verify outputs onchain. Built on Ethereum + IPFS — secure and transparent.
        </p>
        <div className="aur-hero-cta">
          <button className="aur-btn aur-btn-primary">Create Task</button>
          <button className="aur-btn aur-btn-secondary">Explore Tasks</button>
        </div>
      </div>
    </section>
  );
}
